import { colors } from '../tokens/colors';

export const darkTheme = {
  canvas: colors.canvas,
  surface: colors.panel,
  text: colors.ink,
  copy: colors.copy,
  muted: colors.copySoft,
  rule: colors.rule,
  focus: colors.focus,
} as const;
