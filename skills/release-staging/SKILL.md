release-staging/
release-staging/
	•	Staging → Production promotion: SHA binding, required checks, rollback anchor, post-deploy verification.
	•	Writes the corresponding docs/releases/ entry as part of closing — the two are one motion, not two.
