media-generation/
media-generation/
	•	Drop 1–2 real mock/POD images in; get back a merch pack (front/back/side/detail), an on-model pack, an Approximate-3D GLB, and 2 motion clips (walk + product showcase) — via FASHN, Tripo, and Runway respectively.
	•	No Shopify, POD, or publishing action happens automatically — every output is a Draft candidate, truth-labelled (e.g. “Approximate 3D,” never claimed as verified), same discipline as PRD.md's existing rule against inventing unsupported spin/3D/on-model evidence.
	•	Approval split matches everything else in this model: Aarti operates the pipeline; Pushpa (Product Owner) owns the truthfulness call and approves a candidate into the Media Registry. Nothing skips this gate regardless of candidate quality.
	•	Promotion path: candidate → Pushpa approval → Media Registry entry → only then eligible for Shopify upload. The pipeline produces candidates; it does not publish them.
