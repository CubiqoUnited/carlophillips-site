ITEM: CP-CAT-001 — catalogue sizing filter (P1) and hardcoded navigation (P1)
RESULT: ADR WRITTEN, STATUS PROPOSED. No code changed. Build not started — awaiting Pushpa product-fit gate then Sushma readiness gate. One blocking input identified that I cannot obtain: a live authoritative variant read.
EVIDENCE: decisions/ADR-0001-catalogue-sizing-and-navigation.md. Code facts read at HEAD on ship-envgate: apps/web/src/lib/providers/shopify/product-loader.ts:29 (PHASE_ONE_SALE_SIZES), :221-226 (no-Size pass-through), :228-233 (filter), :152-157 + :195-197 + :199-211 (silent degrade to priced-unbuyable); apps/web/src/lib/navigation/storefront-menu.ts:20-33 and :90-92 (resolver clips live categories back onto the two constants); apps/web/src/components/commerce/catalog-state.tsx:108-111,137 and apps/web/src/components/layout/StorefrontHeader/index.tsx:30 (live categories already reach the resolver); apps/web/src/components/editorial/WorkbookReplica.tsx:397-399 vs :860 (live categories computed, then not passed); apps/web/src/lib/commerce/home-catalog-summary.ts:69-81 (category derivation already exists); tests/storefront-menu-navigation.test.tsx:15-17,:47 (tests pin the defect).
NEXT: Pushpa reviews ADR §9 Q1/Q2 (per-product Shopify-declared curation; size vocabulary per category). Sushma obtains the blocking live read: variant selectedOptions for every ACTIVE product on production and staging, with the date read — it decides whether removing the filter is a no-op for the Signature Hoodie or whether a curation metafield must be authored in Shopify first. Not buildable before that.
OWNER: Aarti (ADR + build). Sushma (live read, readiness gate). Pushpa (product-fit gate).
RESUME: on both gates cleared and the live variant read on disk.

VERIFIED FACTS: all file:line citations above, read directly at HEAD on branch ship-envgate, 2026-09-19. They describe deployed code (only apps/web is built — ADR-001).

ASSUMPTIONS (open, must not be graded closed):
- A1 The Signature Hoodie's Shopify record holds only S/M/L. NOW.md:29 says "correctly curated to S/M/L" with 9 variants at $128, but that is a NOW.md statement, not a variant read. If false, deleting the filter exposes suppressed variants on a live-payments store — same class as the Rapid Logo Tee P0.
- A2 No other deployed code re-applies a size restriction. grep found the constant only at product-loader.ts:29,232; not exhaustively traced through variant-presentation-policy. apps/web/src/components/product/ProductForm/index.tsx:23 carries a full XXS-5XL table, suggesting the UI was always built wider than the loader.
- A3 CONTRADICTION, unresolved: NOW.md:33 records the live menu rendering only ALL CATEGORIES and ALL HOODIES, self-corrected from live state with no code change. The code predicts ALL TSHIRTS renders too, from the constant, on the landing page (WorkbookReplica.tsx:860 calls the resolver with no argument). Either the deployed build differs from this tree or the observation was of a different surface. Recorded as a discrepancy to resolve, not as a finding.
- A4 Unknown to me: whether Apliiq supports numeric/footwear sizing. If it does not, this change is still correct and unlocks nothing. Worth answering before the build, not after.
