# SIGNAL — pushpa — KAN-14 (CP-COPY-001 Part 2)

**ITEM:** KAN-14 (CP-COPY-001) — PDP promises "Returns accepted — see policy" with no policy page behind it. P1 LAUNCH, live-payment production store.

**RESULT:** SCOPE NARROWED, NOT WIDENED — the policy text already exists. `docs/policies/CP-POLICIES-v1-2026-09-18.md` is a declared source of truth carrying finished returns and shipping paste blocks, DRAFTED and NOT PUBLISHED (verified 2026-09-19). KAN-14 is a publication + placeholder + link-target gap, not an authoring gap. Delivered: AC-1..AC-12 in `work/items/CP-COPY-001.md` Part 2; approved PDP string `Free returns within 30 days. Shipping is calculated at checkout. Read our shipping policy and returns policy.` with two links; ruling on "eligible" — REMOVE the free-shipping claim from the PDP rather than define eligibility, because the threshold is Boss's open decision and an undefined qualifier is a disputable representation that buys nothing; test assertion stays structural (`cp-purchase-support` + `/shipping/i` + `/returns/i`) plus the permanent final-sale / made-to-order guards. D-030 gates publication of the returns policy and the returns half of the PDP string ONLY — the shipping page, the shipping half, the links and the test fix are not blocked and should proceed now. Return destination written as `[[RETURN_DESTINATION_ADDRESS]]`. NEW BLOCKER SURFACED: `[[DISPATCH_WINDOW]]` is a hard blocker on publishing the shipping page and has no drafted escape hatch the way the threshold does — I do not see it on the board. Raise it to Boss.

**EVIDENCE:** 11 verified facts with sources and the 2026-09-19 date, plus 4 flagged assumptions, tabulated at `work/items/CP-COPY-001.md` §P2.0. Confirming evidence recorded, not only gaps: `apps/web` remains clean of all prohibited fulfilment language; CP-POLICIES-v1 §2.1 already satisfies 9 of the 10 mandatory returns items and §2.2 all 7 shipping items subject to placeholders. Handed back unsolved because they are not mine: whether Shopify `/policies/*` URLs resolve on the headless domain (architecture, Aarti — AC-4 states the requirement, not the mechanism); publication itself (Boss pastes); privacy-policy consistency, unchecked since 2026-09-18.
