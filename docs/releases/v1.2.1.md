# CARLOPHILLIPS site v1.2.1

Status: verified local standards patch.

Baseline: local annotated tag `v1.2` at commit `c01b471`.

## Purpose

v1.2.1 reconciles the frozen v1.2 storefront system with the Product Owner's recovered design-system guidance without redesigning the approved runway and `ONE` Signature Hoodie composition.

The reviewed source is `Recovered-Design-System-Guidance.docx`, SHA-256 `7874be3f6c8c8a1fa7ce782c39538721ece65716882a57dab404e2712d616b5e`. All 18 rendered pages were visually inspected before implementation.

## Included

- one canonical raw-value source at `app/design-tokens.css`;
- strict `--cp-*` naming and a Primitive → Semantic → Component dependency chain;
- explicit colour, typography, spacing, sizing, shape, depth, motion, media, icon, accessibility, responsive, state, and governance contracts;
- active route/component refactors that consume semantic/component roles rather than one-off values;
- documented framework, media-query, dormant-scaffold, and project-independence boundaries;
- deterministic checks for raw-value drift, inline styles, un-tokenized arbitrary utilities, primitive leakage, and representative dependency propagation.

## Deliberately unchanged

- the v1.2 runway image, wording, `ONE` product scene, media inventory, Signature Hoodie identity, price, variants, release gates, checkout policy, and private concept truth boundary;
- external integrations, products, providers, publishing, orders, billing, domains, Preview, and Production;
- the local `v1.2` tag and npm package version.

## Verification

- `yarn verify`: zero-warning lint; 35 test files / 336 tests; zero production dependency vulnerabilities across 193 packages; successful 13-route optimized build.
- Background headless Chrome: eleven direct local desktop/mobile route checks plus media-overlay and reduced-motion checks; all HTTP routes 200 with zero overflow, customer-visible provider names, broken images, console/page errors, or framework overlays.
- Interaction: keyboard focus is visible; the media dialog is responsive, closes with Escape, and reports eleven eligible frames; reduced-motion contexts suppress the campaign and scroll animations.
- Computed tokens: canvas, gutter, card radius, and dialog layer resolve from the canonical source at runtime.
- Visual comparison against v1.2 confirms the runway, `ONE` scene, shop/PDP/bag states, and private concept composition remain materially unchanged.

Evidence: `test_reports/cp-v1.2.1-design-system-standards-2026-08-09/`.

## Deployment boundary

v1.2.1 is local only. No push, pull request, Preview deployment, merge, Production promotion, domain change, commerce/provider write, order, or billing action is included.
