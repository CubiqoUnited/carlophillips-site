# CARLOPHILLIPS site v1.2.2

Status: corrected verified release candidate under review in PR #9. PR #8 merged the superseded candidate to canonical `main` but was not promoted to Production. Production remains unchanged until the exact corrected candidate passes the independent Preview gate, reaches canonical `main`, and is explicitly promoted.

Baseline: clean `e3dc7c2` / v1.2.1 lineage. Approved visible reference: production artifact `bb9568f`.

## Purpose

v1.2.2 corrects and expands v1.2.1. It promotes the design-system architecture without promoting v1.2.1's superseded three-attribute, 11-view, upper-positioned `ONE` experiment. The visible contract remains the approved `bb9568f` composition.

## Included

- one canonical raw-value source at `app/design-tokens.css`;
- enforced lowercase kebab-case `--cp-primitive-*`, `--cp-semantic-*`, and `--cp-component-*` tiers;
- complete colour, type, spacing, sizing, border, shape, depth, motion, media, layout, reset, breakpoint, runtime, accessibility, and stacking coverage;
- one mechanically verified runtime serializer mirror for media conditions, image sizing, metadata, viewport values, and Open Graph rendering;
- active CP-only component classes with no source inline styles, arbitrary utility decisions, primitive leakage, or raw customer visual literals;
- restored four-tag, 12-view, lower-left `ONE` production contract and truthful inset media behavior;
- dedicated Production-parity roles for product facts, commerce body, actions, cards, PDP form/disclosure geometry, Information typography/spacing, and Editorial typography/measure;
- distinct token-led deep-canvas shop/bag and black product-detail page backgrounds, matching the approved Production roles;
- provider-neutral customer-facing media names that preserve truthful AI-assisted meaning without changing canonical provider evidence;
- deterministic naming, tier, closure, reachability, hardcoding, propagation, cleanup, content, media, and layout tests;
- deletion of the exhaustively unreferenced generic UI scaffold and its direct dependencies;
- private package version marker `1.2.2`; the package remains unpublished.

## Deliberately unchanged

- Signature Hoodie identity, handle, price, variants, availability truth, release records, cart/checkout authority, and Shopify source-of-truth boundaries;
- approved runway/media assets and disclosure semantics;
- external products, providers, catalog/order state, domains, Preview, and Production;
- historical release evidence and user assets.

## Verification acceptance

The candidate is acceptable only when frozen install, zero-warning lint, all tests, production dependency audit, optimized build, secret scan, unused-reference audit, and asset decode checks pass. Headless local evidence must cover 1440×1000, 584×486, and 390×844 home hero, `ONE`, overlay, shop, PDP, and bag states; all 12 media views; focus, Escape, arrows/swipe, focus return, reduced motion; zero overflow, browser/framework errors, broken media, or customer-visible provider leakage. Screenshot comparison must preserve the `bb9568f` composition within documented rendering variance.

Evidence: `test_reports/cp-v1.2.2-design-system-release-2026-08-14/`, `test_reports/cp-v1.2.2-production-parity-correction-2026-08-14/`, and `test_reports/cp-v1.2.2-uat-correction-2026-08-14/`.

## Verified result

- Frozen Yarn 1.22.22 install, zero-warning lint, 36 test files / 346 tests, zero production vulnerabilities across 55 audited packages, and the 13-route optimized build pass.
- Repository/secret/reference checks report 886 reachable tokens, five required runtime dependencies, zero unused runtime dependencies, exact duplicate active code, dormant CP selectors, removed-path references, or strong-pattern secret matches. All 26 public visual assets decode/parse.
- Twenty-one headless local screenshots cover the seven required states, including the navigation dialog, at all three required viewports. All checked routes return 200 with zero overflow, provider leakage, console/page errors, request failures, framework overlays, or broken media.
- All 12 gallery assets decode. Arrow navigation, touch swipe, terminal focus containment, Escape, focus return, scroll lock, and reduced-motion behavior pass.
- The live-Production harness passes 222/222 typography, geometry, padding, wrapping, and document-coordinate checks across desktop, compact, and mobile. Its 48 same-dimension captures form 24 Production/candidate comparison pairs; animated/lazy pixel variance is documented and does not override exact DOM geometry and decoded-media acceptance.
- The final UAT harness confirms exact route-specific background parity at all three widths: shop/bag use `rgb(2, 2, 2)` and both PDP states use `rgb(0, 0, 0)`. Candidate accessible names contain zero provider leakage; exact raw generated-media sanitization is enforced at the shared presentation and rendered-PDP boundaries. Twenty-one captures and nine same-dimension route comparisons record the result.
- Saved `bb9568f` comparison passes exact content and geometry acceptance. Viewer pixels are near-identical; hero variance is low and animation-bound; product-image variance is limited to the same runway animation at different capture instants plus the required 11→12 count correction in the saved local evidence.

## Deployment boundary

The corrected release branch is published as PR #9; PR #8 is retained as the historical superseded merge. A manual Preview of the prior correction reached UAT and remained NO-GO because its commerce canvas and two generated-media accessible names did not match the approved customer boundary. The automatic GitHub Vercel `FAILURE` is a fork-policy signal, not runtime evidence. This follow-up does not embed a self-referential commit or deployment identifier: the immutable PR receipt must attach a manual Preview that is explicitly metadata-bound to the resulting Git commit, followed by independent UAT and integration GO. Production promotion remains fail-closed until that exact reviewed candidate reaches canonical `main` and is explicitly promoted. Domains, Shopify writes, catalog/order changes, and billing remain untouched.
