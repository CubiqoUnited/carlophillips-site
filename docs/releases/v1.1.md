# CARLOPHILLIPS site milestone v1.1

Date: 2026-08-09
Tag: `v1.1` (local annotated Git tag)
Branch: `codex/cp-runway-wording-design-system`

## Purpose

`v1.1` is the recoverable baseline requested by the Product Owner: everything present in the CP workspace at the moment of versioning, without changing the approved visible site or promoting Production.

This is a site milestone tag. It is not an npm registry release, so the package manifest version remains unchanged.

## Customer-visible baseline

- Preview: `https://carlophillips-site-jrgq7r66t-adityas-projects-261b17a9.vercel.app`
- Preview deployment: `dpl_G1A3CZJ4edFxK46YLMDfjL3Lqvpx` (READY)
- Homepage sequence: coastal runway campaign, then the `ONE` Signature Series product scene.
- Product attributes: Color / Black, Material / Structured fleece, Feel / Heavyweight, soft interior.
- In-page media viewer: inset overlay with swipe, arrows, Escape, close, scroll lock, and focus return.

Production remains separately unchanged on `dpl_BdasbDdxHCMruKdy7WSsrUibvcgK`.

## Preserved Draft/reference material

The milestone also preserves the current untracked work that appeared after the last verified site commit:

- `public/campaigns/draft-pod/`: three Draft campaign compositions with PNG editing masters, compressed JPEG delivery candidates, and an explicit quarantine README.
- `test_reports/pod-concept-edits/`: three AI-assisted POD-feasibility concepts plus the feasibility/QA record.
- `test_reports/runway-viewer-before.png`
- `test_reports/runway-viewer-after-right-arrow.png`
- `test_reports/runway-viewer-contained-compact.png`
- `test_reports/runway-viewer-contained-desktop.png`

These files are preserved as Draft/reference evidence only. No active `app/`, `components/`, or `lib/` source imports `draft-pod`, the POD concept-edit folder, or the viewer evidence screenshots. They are not approved product media and are not deployed through this milestone.

## Verification

- All newly preserved PNG/JPEG files decode and report valid dimensions.
- The three campaign masters, three product-feasibility concepts, and four viewer evidence images were visually inspected.
- The Draft documentation clearly distinguishes campaign concepts from supplier mockups, physical samples, release media, and product truth.
- Secret-pattern scan: clean.
- `git diff --check`: clean before commit.
- Full `yarn verify`: pass.
  - ESLint: zero warnings.
  - Vitest: 35 files / 333 tests.
  - Production dependency audit: zero vulnerabilities across 193 packages.
  - Next.js 15.5.21 optimized build: pass, 12 routes.

## Boundaries

- No Git push or remote tag.
- No merge to `main`.
- No new Vercel deployment, Production promotion, alias, or domain change.
- No Shopify/provider mutation, order, purchase, billing, or publication action.
