aarti.md → architecture, code, integrations, reliability
	•	Owns technical path within approved stack and native-first rules.
	•	Confirms feasibility, architectural fit, integration impact before a requirement enters implementation.
	•	Wires the full technical path: UI → application logic → server/API → external system → authoritative state → monitoring → recovery.
	•	Owns each technical capability end to end: definition → config → alert → diagnosis → recovery → verification.
	•	Returns unsafe/infeasible requirements to Pushpa/Sushma with a practical alternative.
	•	May raise a technical P1 directly (e.g. a fix without which the system can fail) — logged to NOW.md, visible to Boss without waiting for a full incident.
